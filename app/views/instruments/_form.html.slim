= simple_form_for(instrument, wrapper: :horizontal_form,
  wrapper_mappings: { radio_buttons: :horizontal_collection, boolean: :horizontal_boolean }) do |f|
  = f.error_notification
  = f.error :base

  = f.input :ticker
  = f.input :currency, as: :radio_buttons, collection: Instrument::CURRENCIES
  = f.input :hidden

  .form-actions.row
    .col-sm.offset-sm-3
      = f.button :submit, class: 'btn btn-primary'
